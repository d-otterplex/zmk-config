name: Manually build ZMK firmware
run-name: '#${{ github.run_id }} - ${{ github.ref_name }}: ${{ github.actor }} manually started run'
on: workflow_dispatch

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
